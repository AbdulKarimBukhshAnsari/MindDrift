import './options.css';

/**
 * Settings page — implement:
 * site classifications + notification toggle.
 */
export function OptionsApp() {
  return (
    <main className="options">
      <h1>MindDrift Settings</h1>
      <p className="lede">Minimal settings for site classifications and notifications.</p>
      <p className="placeholder">Options page — implement settings UI here.</p>
    </main>
  );
}
