import './popup.css';

/**
 * Main extension popup — implement MVP UI here:
 * today's insight, focus state, start session, mark site.
 */
export function PopupApp() {
  return (
    <main className="popup">
      <header>
        <h1>MindDrift</h1>
        <p className="tagline">Catch focus loss in the moment.</p>
      </header>
      <p className="placeholder">Popup UI — implement Feature screens here.</p>
    </main>
  );
}
